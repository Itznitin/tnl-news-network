'use client';
import { motion } from 'framer-motion'; import Link from 'next/link';
export default function FeaturedGrid({ items }: any) { const lead = items[0]; const others = items.slice(1,5); return (<div className="grid grid-cols-1 md:grid-cols-3 gap-4">{lead && (<motion.div initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} className="md:col-span-2 bg-white dark:bg-slate-800 rounded-lg overflow-hidden card-hover"><Link href={`/article/${encodeURIComponent(Buffer.from(lead.url).toString('base64'))}`}><div className="relative h-64">{lead.image ? <img src={lead.image} className="w-full h-full object-cover img-hover" alt={lead.title} /> : <div className="w-full h-full bg-slate-200" />}<div className="absolute left-4 bottom-4 text-white"><h3 className="text-2xl font-bold drop-shadow">{lead.title}</h3><div className="text-sm mt-2">{lead.source}</div></div></div></Link></motion.div>)}<div className="grid gap-4">{others.map((it:any,i:number)=>(<motion.div key={i} initial={{ opacity: 0, y: 6 }} animate={{ opacity: 1, y: 0 }} className="bg-white dark:bg-slate-800 rounded-lg overflow-hidden card-hover"><Link href={`/article/${encodeURIComponent(Buffer.from(it.url).toString('base64'))}`}><div className="flex gap-3 p-3"><div className="w-28 h-20 bg-slate-200 overflow-hidden rounded">{it.image ? <img src={it.image} className="w-full h-full object-cover" /> : null}</div><div><h4 className="font-semibold">{it.title}</h4><div className="text-xs text-slate-400 mt-1">{it.source}</div></div></div></Link></motion.div>))}</div></div>) }
